[package]
name = "calculator"
version = "0.9.0"
authors = ["Niko Matsakis <niko@alum.mit.edu>"]
build = "build.rs" # <-- We added this and everything after!

[build-dependencies]
version = "0.9.0"

[dependencies]
version = "0.9.0"
