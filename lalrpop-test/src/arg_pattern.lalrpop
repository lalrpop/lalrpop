grammar;

Or<A, B> = { A B, B A }
DeepOr<A, B, C> = {
    A Or<B, C>,
}

pub Foo: String = {
    <(a, b):Or<"a", "b">> => {
        a.to_string() + b
    },
    <(a, (b, c)):DeepOr<"A", "B", "C">> => {
        a.to_string() + b + c
    },
};
