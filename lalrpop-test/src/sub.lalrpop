grammar;

use util::tok::Tok;

extern token {
    enum Tok {
        "(" => Tok::LParen(..),
        ")" => Tok::RParen(..),
        "-" => Tok::Minus(..),
        "Num" => Tok::Num(<i32>),
    }
}

pub S = E;

E = {
    <l:E> "-" <r:T> => l-r;

    T;
};

T = {
    <"Num">;
    "(" <E> ")";
};

